node_modules/
backend/node_modules/
frontend/node_modules/
frontend/build/

# Environment — NEVER commit real credentials
backend/.env

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
tweller-flow.backup/

# Plugin build output — rebuild with `npm run build:plugin`, never commit
# a zip. A hand-committed one (tweller-flow.zip) used to sit in the repo
# root, went stale, and kept colliding with `git pull`.
/dist/
*.zip
